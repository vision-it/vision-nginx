---
vision_nginx::vhost_defaults: {}
vision_nginx::conf_dir: '/vision/etc/nginx'
vision_nginx::vhosts:
  redirect_https:
    listen_port: 80
    listen_options: 'default_server'
    ssl_redirect: true
